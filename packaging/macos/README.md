This directory contains macOS packaging assets for PyInstaller.

- `talktovibe_app.spec` builds `TalkToVibe.app`
- `talktovibe_configure.spec` builds the terminal configure helper

Both specs are intended to be run from the repository root using the project's
virtualenv.
